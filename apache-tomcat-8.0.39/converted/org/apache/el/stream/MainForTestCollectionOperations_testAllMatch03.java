package org.apache.el.stream;

import org.apache.el.stream.TestCollectionOperations;

public class MainForTestCollectionOperations_testAllMatch03 {
    public static void main(String[] args) {
        try {
            TestCollectionOperations objTestCollectionOperations = new TestCollectionOperations();
            objTestCollectionOperations.testAllMatch03();
        } catch (Throwable e) {
            e.printStackTrace();
        }
    }
}
